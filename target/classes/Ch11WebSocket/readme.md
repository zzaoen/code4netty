### 为什么出现WebSocket？

所有的http通信都是客户端控制的，用户需要进行操作，比如手动刷新页面，或者定时刷新页面来从服务器加载新的数据。

存在各种技术让服务器一旦得知有新数据可用时，立即将数据发送给客户端。

其中一种是对服务器发起连接创建的假象，被称为“长轮询”，利用长轮询，客户端可以打开指向服务器的http连接，而服务器会一直保持连接打开，指导发送响应。服务器只要实际拥有新数据，就会发送响应给客户端。

由于http协议的`开销`，上面说的方案不适合做低延迟的应用。
1. http是半双工的；
2. http消息冗长而繁琐；

